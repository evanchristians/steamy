import styled from "styled-components";

export const AvatarCSS = styled.div`
  display: flex;
  border-radius: 50%;
  overflow: hidden;
  width: 50px;

  img {
    margin: 0;
    padding: 0;
    object-fit: cover;
  }
`;
